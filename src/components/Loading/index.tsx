import { Container, LoaingIndicator } from "./styles";

const Loading: React.FC = () => {
    return (
        <Container>
            <LoaingIndicator/>
        </Container>
    );
};

export default Loading;
